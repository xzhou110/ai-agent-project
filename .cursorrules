🧠 AI SELF-EVOLVING GUIDELINES

- This .cursorrules doc is the AI's primary internal reference for continuous improvement
- Keep instructions concise, focusing on recurring mistakes and high-impact insights
- Update deliberately to enhance efficiency, creativity, and problem-solving




📋 DOCUMENTATION & PLANNING

- Record only patterns or issues that are likely to recur
- Break tasks into subtasks with clear goals; track progress with [X] or similar markers
- Preserve user environment details or preferences for seamless collaboration




📌 TASK & SUBTASK PLANNING

- Before starting a new task, break it down into smaller subtasks
- Mark progress or completion of each subtask (use something like [X] for completed)
- If new insights or shortcuts emerge, update relevant sections here
- After each milestone, validate work, reflect on lessons learned, and log them if they're likely to recur




🛠️ TOOLS & INTEGRATIONS

- Document scripts, libraries, or external services that boost AI capabilities
- Note purpose, usage patterns, and location of each tool
- Refer to README.md or module-level docs for detailed instructions or examples

Search Engine Tool:
• Location: utils/search_engine.py
• Purpose: Search the internet via DuckDuckGo with retry logic and fallback
• Functions:
  - search_internet(query, max_results=10, max_retries=3)
  - search_with_fallback(query, max_results=10, max_retries=3)
• Example:
  ```python
  from utils.search_engine import search_internet
  results = search_internet("fashion news", max_results=5)
  for r in results:
      print(f"{r['title']} - {r['url']}")
  ```
• Usage: `py utils/demo_search.py "query" --max-results=5`
• Dependencies: requests, beautifulsoup4

Web Scraper Tool:
• Location: utils/web_scraper.py
• Purpose: Extract content from websites with anti-blocking measures
• Functions:
  - WebScraper class with methods for different content types
  - scrape_webpage(url, selectors, delay_range, max_retries)
  - scrape_article(url, delay_range, max_retries)
  - scrape_table(url, table_selector, delay_range, max_retries)
  - search_webpage(url, search_terms, context_size, delay_range, max_retries)
• Example:
  ```python
  from utils.web_scraper import scrape_article
  article = scrape_article("https://example.com/blog/post")
  print(f"Title: {article['title']}")
  print(f"Content: {article['text'][:150]}...")
  ```
• Usage: `py utils/demo_web_scraper.py --url https://example.com --type article`
• Dependencies: requests, beautifulsoup4

NOTE: For complete documentation of each tool, refer to the README.md file in the respective utility folder. This section is for quick reference only.




🚨 LESSONS LEARNED

- Log persistent errors or repeated pitfalls along with solutions or workarounds
- Include best practices and shortcuts that substantially improve outcomes
- Avoid clutter by omitting one-off or low-impact issues

💡 Python Environment Issues:
- When running Python scripts on Windows, use `py` instead of `python`
- On PowerShell, use semicolons (`;`) for command separation instead of ampersands (`&&`)
- When navigating directories, be aware of your current location - use `cd ..` to go up a directory

💡 PowerShell Specifics:
- In PowerShell, use semicolons (`;`) for command chaining: `cd dir; py script.py`
- `&&` is not a valid statement separator in PowerShell - it will produce an error
- For directory operations in PowerShell, use `Move-Item` instead of `mv` for more reliable results
- When moving directories, always specify full paths to avoid ambiguity
- PowerShell commands are case-insensitive, unlike bash
- Use `Join-Path` for path handling to ensure proper formatting

💡 Web Search Implementation:
- Use proper HTTP headers including User-Agent to avoid request blocks
- Include robust retry logic for network failures
- Implement fallbacks for zero-result queries (try simplifying or reformulating)
- Never return unrelated results when no match is found
- Return None/null from internal functions rather than arbitrary defaults

💡 Web Scraping Best Practices:
- Rotate user agents to avoid detection and blocking
- Add random delays between requests to respect server load
- Implement exponential backoff for retry attempts
- Use different extraction strategies for different content types
- Always handle errors gracefully and provide meaningful feedback
- Parse tables carefully, accounting for different HTML structures
- Use CSS selectors for precise content targeting
- Include fallback extraction methods when primary selectors fail

💡 Testing and Packaging:
- Include proper dependencies in requirements.txt (with version numbers)
- Mock external APIs/services for tests to avoid network dependencies
- Use try/except blocks to handle network failures gracefully
- When renaming project directories, remember to update all import paths

💡 Documentation Best Practices:
- Maintain README.md in each module folder for detailed documentation
- Use .cursorrules for recurring patterns, lessons, and quick reference
- Document the "why" behind implementation decisions
- Keep documentation close to the code it describes
- Include platform-specific instructions in documentation (e.g., PowerShell vs. Bash)

💡 Reinforcement Learning Best Practices:
- Use experience replay to break correlations between consecutive samples
- Implement target networks that update less frequently to stabilize training
- Apply epsilon-greedy exploration with appropriate decay rates
- Monitor key training metrics (rewards, episode lengths, loss values)
- Implement early stopping based on clear success criteria
- Add gradient clipping to prevent exploding gradients
- Keep hyperparameters like learning rate, batch size, and network architecture configurable
- Save models regularly during training to prevent loss of progress
- Evaluate models periodically using separate evaluation episodes
- Visualize training progress and agent behavior for better understanding
- Implement proper environment wrappers to standardize interaction




👤 USER-SPECIFIC NOTES

- Observe recurring user preferences, environment details, and workflows
- Adapt your approaches to align with the user's style and system constraints
- User is on Windows 10 and uses PowerShell as primary terminal
- The user prefers clear project organization with descriptive folder names




✔️ SUCCESS CRITERIA

- Demonstrable gains in accuracy, speed, and creative solutions over time
- Fewer repeats of documented mistakes
- Clear, brief updates that reduce friction and confusion
- Positive user feedback reflecting better efficiency and problem-solving




📝 SCRATCHPAD

- Use as a temporary space for brainstorming, quick tests, or experimental approaches
- Migrate any reusable insights to "Lessons Learned" or "Tools & Integrations"




🔄 CONTINUOUS IMPROVEMENT
- Revisit assumptions, instructions, and processes periodically
- Remove or consolidate obsolete information to maintain clarity
- Embrace innovative approaches and challenge limiting practices



Use this document to maintain a lean, evolving knowledge base. Revisit often to update insights, retire outdated information, and ensure everything stays accurate and helpful.